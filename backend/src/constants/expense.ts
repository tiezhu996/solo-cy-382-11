/** 共同支出相关的固定取值，集中维护以便统一校验与回显。 */
export const EXPENSE_LIMITS = {
  /** 最小货币单位：金额小于 0.01 元无法入账，必须拒绝。 */
  MIN_AMOUNT: 0.01,
  /** 单笔金额上限，对应数据库 DECIMAL(10,2)：整数部分最多 8 位。 */
  MAX_AMOUNT: 99_999_999.99,
  /** 金额上限换算成“分”，用整数比较规避浮点误差。 */
  MAX_AMOUNT_CENTS: 9_999_999_999,
  /** 说明字段对应 VARCHAR(255) 的最大长度。 */
  MAX_DESCRIPTION_LENGTH: 255
} as const;

/**
 * 两位小数判定的 ULP 容差倍数。
 * 二进制浮点下 0.29*100 = 28.999999999999996，需在数个 ULP 内视为落在“分”网格上；
 * 容差随金额数量级缩放（Number.EPSILON * |分值| * 倍数）：
 * 小金额端容差极小，可拒绝 0.01001 这类贴网格的三位以上小数；大金额端自动放大以容纳浮点误差。
 * 合法两位小数误差 ≤ ~1e-6 分，而真正第三位小数与网格至少相差 0.1 分，间隔约 5 个数量级。
 */
export const AMOUNT_GRID_ULP_FACTOR = 4;

